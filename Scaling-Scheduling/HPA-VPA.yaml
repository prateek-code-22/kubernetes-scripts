apiVersion: v1
kind: Pod
metadata:
  name: stress-test
spec:
  containers:
  - name: stress-container
    image: polinux/stress
    command: ["stress"]
    resources:
      requests:
        memory: "50Mi"
      limits:
        memory: "100Mi"
    args: ["--vm", "1", "--vm-bytes", "80M", "--timeout", "10s"]
  